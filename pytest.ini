[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Specify the order of test files
# Run the problematic tests first, then the rest
addopts = --verbose
